interface:
  display_name: "Act"
  short_description: "Repo-specific patterns and workflows for act"
  default_prompt: "Use the act repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true